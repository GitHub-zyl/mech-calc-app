# 机械设计常用计算表 — 部署说明

## 方式一：直接运行（开发/单机）

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 启动
python backend/app.py

# 3. 浏览器打开
# http://127.0.0.1:9090
```

## 方式二：打包为独立 EXE（推荐离线使用）

```bash
pip install pyinstaller

pyinstaller --onefile --add-data "templates;templates" --add-data "static;static" --add-data "data;data" --name "机械设计计算表" backend/app.py
```

生成的可执行文件在 `dist/机械设计计算表.exe`，双击即用。

## 方式三：生产服务器部署 (Windows)

### 使用 Gunicorn (需要 WSL/Cygwin)

```bash
# 安装
pip install gunicorn

# 启动
gunicorn -w 2 -b 0.0.0.0:9090 backend.app:app
```

### 使用 Nginx 反向代理

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:9090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 静态文件缓存
    location /static/ {
        proxy_pass http://127.0.0.1:9090/static/;
        expires 7d;
    }
}
```

### 注册为 Windows 服务

使用 `nssm` (Non-Sucking Service Manager):

```bash
nssm install 机械计算表 "C:\Python312\python.exe" "C:\机械计算小程序\backend\app.py"
nssm start 机械计算表
```

## 文件结构

```
机械计算小程序/
├── backend/
│   ├── app.py              # 主入口（启动此文件）
│   ├── calculations/       # 计算模块
│   └── extract_data.py     # 数据提取脚本
├── static/                 # 前端静态文件
├── templates/              # HTML模板
├── data/                   # 材料数据库
└── requirements.txt        # 依赖清单
```

## 端口配置

默认端口 `9090`，如需修改：

```python
# backend/app.py 最后一行
app.run(host='127.0.0.1', port=9090, debug=True)
```
